<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.craigjmart.imagefinder.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock.Light.DarkActionBar" >
        <activity
            android:name="in.craigjmart.imagefinder.app.ImageSearchActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="in.craigjmart.imagefinder.app.ImageDisplayActivity"
            android:label="@string/app_name" >
            <!--android:parentActivityName="in.craigjmart.imagefinder.app.ImageSearchActivity" > -->
            <!-- Parent activity meta-data to support API level 7+ -->
            <!--<meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="in.craigjmart.imagefinder.app.ImageSearchActivity" /> -->
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
